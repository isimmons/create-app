import { it, expect } from 'vitest'

// I don't know how or what to test for a npx script like this yet
// but tests are in the CI pipeline so...
it('should work', () => {
  expect(true).toBe(true)
})
